/*===============================================================
*   Copyright (C) 2015 All rights reserved.
*   文件名称：main.cpp
*   创 建 者：pengcheng
*   创建日期：2015-08-06
*   描    述：
================================================================*/
#include <stdio.h>
#include <map.h>

int main(){
#ifdef ONLINE_JUDGE
#else
    freopen("in.txt","r",stdin);
#endif
    char buf[50],fir[20],sec[20];
    memset(buf,0,sizeof(buf));
    memset(fir,0,sizeof(fir));
    memset(sec,0,sizeof(sec));
    scanf("%s",fir);

    while(strcmp(buf,"END")!=0){
    }
}
